<% content_for :title, "Quien es Mas Probable - Fun-able" %>

<div class="min-h-screen relative overflow-hidden py-8 px-4">
  <!-- Animated Background -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-0 left-0 -translate-x-1/2 w-64 h-64 md:w-[500px] md:h-[500px] bg-purple-500/15 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute top-1/3 right-0 translate-x-1/2 w-80 h-80 md:w-[500px] md:h-[500px] bg-pink-500/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 1s;"></div>
    <div class="absolute bottom-0 left-1/2 -translate-x-1/2 translate-y-1/2 w-96 h-96 md:w-[600px] md:h-[600px] bg-cyan-500/10 rounded-full blur-3xl animate-pulse" style="animation-delay: 2s;"></div>
  </div>

  <div class="relative z-10 max-w-lg mx-auto">
    <!-- Header -->
    <div class="text-center mb-8">
      <div class="inline-flex items-center justify-center w-20 h-20 rounded-full mb-4"
           style="background: linear-gradient(135deg, rgba(168, 85, 247, 0.2) 0%, rgba(236, 72, 153, 0.1) 100%);
                  border: 2px solid rgba(168, 85, 247, 0.3);
                  box-shadow: 0 0 30px rgba(168, 85, 247, 0.2);">
        <span class="text-4xl">🎯</span>
      </div>
      <h1 class="text-3xl md:text-4xl font-black text-white mb-2"
          style="text-shadow: 0 0 20px rgba(168, 85, 247, 0.4);">
        Quien es Mas Probable
      </h1>
      <p class="text-white/60">
        Voten por quien es mas probable que haga algo... el mas votado bebe!
      </p>
    </div>

    <!-- Form Card -->
    <div class="rounded-2xl p-6 md:p-8"
         style="background: linear-gradient(145deg, rgba(15, 15, 15, 0.9) 0%, rgba(20, 20, 20, 0.95) 100%);
                border: 1px solid rgba(168, 85, 247, 0.2);
                box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);">

      <%= form_with model: @game, url: most_likely_to_index_path, method: :post, class: "space-y-6" do |f| %>

        <!-- Total Rounds -->
        <div>
          <label class="block text-sm font-medium text-purple-400 mb-2">
            Numero de Rondas
          </label>
          <div class="relative">
            <%= f.select :total_rounds,
                options_for_select((5..20).map { |n| [n, n] }, 10),
                {},
                class: "w-full px-4 py-3 rounded-xl text-white appearance-none cursor-pointer",
                style: "background: rgba(20, 20, 20, 0.9); border: 1px solid rgba(168, 85, 247, 0.3);" %>
            <div class="absolute right-4 top-1/2 -translate-y-1/2 pointer-events-none">
              <svg class="w-5 h-5 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Max Players -->
        <div>
          <label class="block text-sm font-medium text-purple-400 mb-2">
            Maximo de Jugadores
          </label>
          <div class="relative">
            <%= f.select :max_players,
                options_for_select((3..15).map { |n| [n, n] }, 10),
                {},
                class: "w-full px-4 py-3 rounded-xl text-white appearance-none cursor-pointer",
                style: "background: rgba(20, 20, 20, 0.9); border: 1px solid rgba(168, 85, 247, 0.3);" %>
            <div class="absolute right-4 top-1/2 -translate-y-1/2 pointer-events-none">
              <svg class="w-5 h-5 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Rules Preview -->
        <div class="p-4 rounded-xl" style="background: rgba(168, 85, 247, 0.1); border: 1px solid rgba(168, 85, 247, 0.2);">
          <h3 class="font-bold text-purple-400 mb-2 flex items-center gap-2">
            <span>📋</span> Como jugar
          </h3>
          <ul class="text-sm text-white/70 space-y-1">
            <li>1. Aparece una carta con "Quien es mas probable..."</li>
            <li>2. Todos votan por alguien del grupo</li>
            <li>3. El mas votado bebe!</li>
            <li>4. Si hay empate, todos los empatados beben</li>
          </ul>
        </div>

        <!-- Submit Button -->
        <button type="submit"
                class="w-full py-4 rounded-xl font-bold text-white text-lg transition-all hover:scale-[1.02] active:scale-[0.98]"
                style="background: linear-gradient(135deg, #a855f7 0%, #ec4899 100%);
                       box-shadow: 0 0 20px rgba(168, 85, 247, 0.4), 0 4px 20px rgba(0, 0, 0, 0.3);">
          Crear Partida
        </button>
      <% end %>
    </div>

    <!-- Back Link -->
    <div class="text-center mt-6">
      <%= link_to "Volver al inicio", root_path, class: "text-white/50 hover:text-white transition-colors" %>
    </div>
  </div>
</div>
